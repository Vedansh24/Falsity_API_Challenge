# Test database (use a dedicated DB, never production)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/falsity_test
JWT_SECRET=test-jwt-secret
NODE_ENV=test
PORT=3331
CORS_ORIGIN=http://localhost:3000
