// Investigations routes entrypoint
import type { FastifyInstance } from 'fastify';

export default function registerInvestigationRoutes(_fastify: FastifyInstance) {
  // placeholder for investigation routes
}
